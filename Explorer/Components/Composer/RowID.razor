<div class="row-id">
    @DisplayedName
</div>

@code {
    [Parameter]
    public string Name { get; set; } = "";

    private string DisplayedName => (Name.Length > 8) ? $"{Name.Substring(0, 6)}.." : Name;
}
